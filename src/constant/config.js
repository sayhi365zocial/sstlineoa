export const isLiffGuardActive = true
export const isAuthGuardActive = true

export const mockUser = {
  username: 'skeletonadmin',
  token: 'ยิงจริงไมไ่ด้นะ token อันนี้'
}

export const privateRoot = '/report'
